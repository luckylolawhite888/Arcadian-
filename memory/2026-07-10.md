# Emma Update 2026-07-10

## Apollo Integration Status

### What's Working
- **/api_search** endpoint works with `q_organization_domains[]`, `person_titles[]`, `q_keywords` URL params
- **/organizations/search** works with keyword filter body params
- **/people/match** works with `first_name+last_name+domain` and `reveal_personal_emails=true` — returns real emails (tested: greg.jackson@octoenergy.com, sundar@google.com, zoisa.walton@octoenergy.com)
- **Enrichment returning real emails** for known execs at recognizable companies
- **Gmail auto-send** working (tested: email sent to tony@eco-emma.com for Tony Test)

### Issues
1. **`/api_search` returns people without org domains** — even when searching known domains, many results have empty `primary_domain`. Enrichment needs domain to return emails.
2. **Unicode `…` (U+8230) in API key** — the key `y9rZYH…EO8w` has a unicode ellipsis character, not three ASCII dots. This might cause the ByteString error we saw (`character at index 6 has value 8230 > 255`).
3. **`enrichUrl` uses `let` with `+=` for appending orgName** — causes "Assignment to constant variable" error. FIXED in latest deploy.
4. **Two-step pipeline not implemented** — need to search orgs → get domains → search people by domain → enrich with domain

### Next Steps
1. Fix API key — replace unicode ellipsis with three ASCII dots in `.env` and server.js
2. Implement two-step pipeline: search orgs by industry/keywords → find people at those orgs by domain → enrich with domain
3. Add `draftLead()` to save enriched leads as drafts in the approvals table
4. Test end-to-end with real solar industry companies

### Notes
- Emma's architecture is solid — leads → drafts → approval → auto-send → campaign threads
- Gemini/DeepSeek email generation is the email prompt in `doResearch()`
- Gmail SMTP on `missecoemma@gmail.com` working
- All 3 core features (campaign threads, send-as-email, auto-send) verified working

### Companies House Integration (GIT: commit 9ad459e, branch `main`)
- CH API key (`1da26254-bbd5-4eec-b64b-a5e2d40f0df7`) now declared in server.js
- `searchCompanies()` and `getCompanyProfile()` functions at module scope
- CH pipeline step added in `doResearch()` — searches UK companies by industry keyword BEFORE Apollo org search
- Finds ~22 UK companies (e.g. SOLAR PANEL INSTALLERS LTD) per search
- **Limitation:** Apollo has no data on most `.co.uk` small/medium companies, so CH-found companies yield 0 enriched contacts
- **PUSH ISSUE:** Git remote is HTTPS to `github.com/missecoemma-droid/emma-backup-.git` with no credential helper or token stored. Commit is local on the VPS but cannot push to GitHub.
- **Files committed locally:** server.js (CH integration, multi-target search, API key), index.html (mock data cleanup, UI polish), .gitignore, .env
