# 2026-06-24 — Out & About v8 + Darren Pelling Email

## Session Summary
Maya wanted to replace the Out & About frontend with her new v8 design, then have backend wired in (Supabase auth, deals, favourites, stores + free API endpoints).

## Done
- [x] Maya uploaded the new v8 HTML (203KB, "outandabout v8.html") via SCP from her laptop → `/var/www/07542666646.com/public/out-and-about/index.html`
- [x] Lola confirmed the file on the server, read the full contents to understand the data structures
- [x] Lola created `frontend-integration.js` — 447-line JS module that wires:
  - Supabase client init (project `mdleurcenwmmenvkwjhl.supabase.co`)
  - Auth (email signup/login/logout replacing mock doLogin/doRegister)
  - Real deals from Supabase `deals` table (fallback to mock DEALS array)
  - Real stores from API `/outandabout-api/stores`
  - Favourites from Supabase `favourites` table (add/remove/load)
  - Free API endpoints: BTC, quote, UK holidays, geocode, QR code
  - Deal publishing POST to Python API server
  - Graceful fallback to mock data on any API failure
- [x] Deployed: `index.html` + `frontend-integration.js` both served from `/var/www/07542666646.com/public/out-and-about/`
- [x] Verified site loads at https://07542666646.com/out-and-about/ (200 OK)

## Darren Pelling Email
- **From:** Darren Pelling → luckylolawhite@gmail.com
- **Subject:** GPM
- **Body:** "fyi"
- **Attachment:** `GPM_Investor_Overview_2026_Final_Plain_Content V3.pdf` (327KB)
- **Summary:** Equity raise for Green Planet Makers Ltd — £200k for 15%. Company connects UK home retrofit installers with carbon credit market. 3 founders (Leigh Goodman, Attila Preszter, Darren Pelling). Current revenue £76-90k/yr from 10 installers. Funds for scaling to 100+ installers. EIS relief unconfirmed.
- **Action:** Maya just wants awareness at this point — Darren sharing info, not a specific ask.

## Key Decisions
- Backend integration script is separate from the HTML (loaded via `<script src="frontend-integration.js">`) so Maya can update her HTML without breaking connectivity
- Python API server at `/opt/outandabout/outandabout_server.py` port 8790 continues running

## Notes
- The new v8 HTML has GSAP stickman animations, beautiful deal cards, tag system, night owl mode — it's a substantial UI upgrade from the previous version
- All mock data kept as fallback in the HTML for offline/demo mode
- Supabase project: mdleurcenwmmenvkwjhl.supabase.co (anon key vaulted)

## Late Update — Supabase Restored & v2 Integration Live (20:00 UTC)
- **Supabase project was INACTIVE** (auto-paused after 7 days free tier inactivity)
- Restored via Management API — `POST /v1/projects/.../restore`
- Took about 2 minutes to go from INACTIVE → COMING_UP → RESTORING → ACTIVE_HEALTHY
- **Keys were rotated after restore** — got new keys from management API:
  - anon JWT (new): eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
  - service_role JWT (new): eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
- Updated `supabase.json` vault with new keys
- **Tables checked:** stores (7 rows ✅), deals (0 — empty after pause), favourites (0), deal_redemptions (0), store_memberships (0), deal_views (0)
- **Server updated:** `outandabout_server.py` fallback key replaced with new service_role JWT, server restarted with new env
- **Frontend updated:** `frontend-integration-v2.js` anon key replaced with new JWT
- **Full stack verified:** site 200, JS 200, api/btc 200, api/stores 200, supabase/stores 200
- **Stripe key found:** In old SMS server code (`sk_live_...`), saved to `.vault/stripe.json`
- **Checklist status:** Most wiring already done in v2 integration file (auth, deals, favs, views, stores API, BTC/quote/holidays/geocode/QR endpoints) — remaining are real-time subscriptions, OneSignal push, role enforcement, Stripe billing
