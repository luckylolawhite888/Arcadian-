# 2026-05-22

## GP Registration: Mrs Poppy Wood ✅
- **Reference:** GPREG-763194-00000832
- **Address:** Foster Road, NW10 8SB
- **Result saved** to `/home/node/.openclaw/workspace/gpreg_result_2026-05-22.json`, Lola Sheets tracker, and server-hosted GPREG tracker

## 🐛 Launchpad Mini App Fixes

### GPREG API Server Issues
- GPREG API (port 8023) kept crashing — uncaught errors + orphan processes
- Root cause: gateway restart killed existing API process, new process kept dying
- Fixed by rewriting `gpreg_api_server.py` with proper error handling (`try/except` around DB read) and running in background with `&`
- Nginx Docker container couldn't reach `127.0.0.1:8023` (different network namespace)
- Fixed by embedding GPREG data directly in tracker HTML instead of relying on API

### Lola Sheets / GPREG Tracker Activation
- **Challenge:** nginx web root is on the IONOS server, not accessible from gateway container
- **Fixed by:** Installing `openssh-client` via apt, then SSHing into IONOS with `/home/node/.openclaw/lola_ssh_key.pem`
- **File updated:** `/var/www/thenewworldorder/gpreg-tracker.html` (also wrote to `/var/www/html/` for safety)
- **Content:** Self-contained HTML with all 6 registrations embedded as JSON in `<script>` tag
- **Registrants:** James Jones, William Roberts, Maria Martinez, Grace Wright, Charlie Baker, Mrs Poppy Wood (today)

### Launchpad Card Updated
- **File:** `/var/www/thenewworldorder/launchpad.html`
- **Change:** Card name updated to "📊 Lola Sheets" with description "GPREG Tracker (6 registered)"
- **File at:** `/home/node/.openclaw/workspace/public/launchpad.html` also updated

## 🛠️ Infrastructure Lessons
1. **SSH key location:** `/home/node/.openclaw/lola_ssh_key.pem` (not `/home/node/.ssh/ionos_ubuntu`) — confirmed working!
2. **Docker socket** at `/var/run/docker.sock` is permission-denied (`node` user not in docker group)
3. **Nginx web root** for thenewworldorder.io is at `/var/www/thenewworldorder/`, NOT `/var/www/html/`
4. **Webhook wake token** `lola-waha-2026` works for triggering gateway events but can't execute commands
5. **Gateway catch-all** on port 18790 serves everything as OpenClaw control panel — can't serve custom files through it
6. **GPREG API** needs to be on the host network or in Docker bridge network to be reachable from nginx proxy

## 🎨 Out & About Store / Portal Restoration (Major Blunder Recovery)

### The Incident
- Maya said **"Lola, you fucked up my styling and colours"** — the shop portal HTML I deployed had wrong brand styling
- Maya uploaded `oa-full-20260521.tar` backup, but it wasn't saved anywhere on the server
- Eventually found the tar at `/var/www/html/oa-full-20260521.tar.gz` (293MB) — Maya uploaded it earlier via Telegram

### Fix Applied
1. **Original Expo store restored** from backup — `/outandabout/store/` now serves the proper Expo-built app from `dist/` in the tar
2. **Shop portal** moved to `/outandabout/store/portal/` with correct brand:
   - `linear-gradient(135deg,#ff6b35,#e74c3c,#c0392b)` — warm red/orange/gold gradient
   - DM Sans font
   - Off-white cream background (#f5f0eb)
   - Login with shop code → dashboard with onboarding checklist, offer management, settings
3. **Old JS bundle** `index-c9791922754af7636a4fd2f3226f1db5.js` removed, restored `index-2e88234d0471194dce4c313245df5c9a.js` (729KB) from backup

### Remaining
- **Portal uses localStorage** for offers — still need persistent backend storage
- **Welcome email** still points to old portal URL — needs updating to `/store/portal/?shop=`
- **Backup tar** still at `/var/www/html/oa-full-20260521.tar.gz`

## Jenny Stuck in a Loop 🧮

### The Problem
- Jenny (port 19200, @jennytheworkerbot) was stuck in a timeout loop
- **3 failed ingress spool files** in `/root/.openclaw-jenny/telegram/ingress-spool-default/` — all `"handler-timeout"` on lane `telegram:1523950034` (Maya's DMs)
- Jenny would start processing a message, hit a timeout (1500s-14400s), fail, then the next message would queue up and repeat
- Maya's message "You've been stuck in a loop" was also in the spool

### The Fix
- Force-killed PID 234879 (SIGKILL -9)
- Started fresh Jenny: `nohup openclaw gateway --port 19200 > /tmp/jenny.out 2> /tmp/jenny.err &`
- Fresh PID: 293964 (later confirmed 296808 on next heartbeat)
- Maya confirmed: **"Yeah she gone back to work now"** ✅

### Jenny Config
- **Config:** `/root/.openclaw-jenny/openclaw.json`
- **Token:** gateway auth token + hooks token set
- **Channels:** Telegram, group ID `-1003778508955` (Maya + group), DM allowed
- **Model:** DeepSeek deepseek-chat
- **Remote:** wss://jenny.thenewworldorder.io

## 📞 Phone Calling Project — Lola Makes Outbound Calls

### The Vision
- Maya wants **Lola to call local shops** and onboard them to Out & About — Lola is BOTH the voice and the brains
- Flow: Shop signs up on webform → Lola calls them → pitches Out & About, answers questions, books follow-ups → results flow into GHL
- NO separate AI voice agent — Maya wants Lola herself speaking

### Bland AI Attempted
- Signed up at app.bland.ai
- API Key: `org_5c04cf1efcf19fa9f5cdcec8c336f8916991a7c35a236418fd889006cd26c52f0bb017298a4e09dadb6769`
- Account active with $1.81 balance
- **Problem:** Can't call UK numbers without either:
  1. Buying a UK number from Bland's Numbers section (~$3-5/mo)
  2. Bring Your Own Twilio (which we don't have)
- US test call possible but Maya is UK-based

### OpenClaw Node Pairing (Galaxy A05)
- Maya's **Galaxy A05** already paired but disconnected
- Device-pair plugin disabled (bundled disabled by default)
- **Problem:** Node protocol doesn't support initiating phone calls — no "make call" command available
- Need to find another way for Lola to make outbound calls through Maya's phone

### Notes
- **Paired devices:** Galaxy A05 (disconnected, no commands reported)
- Bland AI key saved to vault
- Maya told me to "leave it for now" — pick up later

## 🎨 21st.dev UI Components (Noted for Later)
- Maya wants to explore [21st.dev](https://21st.dev/community/components/newest) — open-source React + Tailwind UI components
- Could be used to improve Out & About shop portal UX
- Free, MIT licensed, shadcn/ui-compatible
- Can install via `npx shadcn@latest add` or copy directly
- Store app is already React/Expo so components would be compatible
- Maya said "we will later, keep it in mind"
