
## Emma/Tony VPS
- New client Tony, AI assistant named "Emma"
- VPS: 212.227.38.78, root password: 3v3fUeTROhIl4n
- Telegram-only, no WhatsApp
- Same dashboard template as Scarlett

## Emma/Tony VPS
- New client Tony, AI assistant named "Emma"
- VPS: 212.227.38.78, root password: 3v3fUeTROhIl4n
- Telegram-only, no WhatsApp
- Same dashboard template as Scarlett

## Completed — Emma Setup (2026-07-08 14:48-15:10 UTC)
- **Dashboard deployed** — Scarlett's v4 template customised for Emma (eco-emma.com)
- **Base server** — Ubuntu 24.04, Node 22, nginx, PM2
- **API server** — Express on port 3100, DeepSeek chat + Telegram webhook + News API
- **OpenClaw gateway** — Installed, Telegram bot token configured, running
- **Cloudflare DNS** — eco-emma.com → 212.227.38.78 (proxied, Full SSL)
- **SSL** — Let's Encrypt via certbot (expires Oct 6, auto-renewal setup)
- **ElevenLabs voice** — API key wired, GET /api/voice/voices and POST /api/voice/speak live (Alice voice, British female)
- **Telegram webhook** — POST /api/telegram-webhook routes to DeepSeek, replies via Bot API
- **Access code:** @TONY2026
- **Maya's hard rule:** Never touch Scarlett's VPS without explicit confirmation

## Key details stored
- Cloudflare zone for eco-emma.com under missecoemma@gmail.com
- ElevenLabs voice: sk_193…69c0 (Alice voice by default)
- Telegram: @eco_emma_bot

## Pending — Emma (as of 15:40 UTC)
- **GitHub push:** Needs token from Maya — she'll do it when back at office
  - Create classic token at github.com/settings/tokens with `repo` scope
  - Then run: `cd /var/www && git push -u origin main`
- **Supabase:** Keys wired into server.js (anon + service_role), ready for tables/queries
- **Apollo API key:** Still missing (Tony needs this for lead enrichment)

## Emma Completed (2026-07-08 17:48-18:20 UTC)
- **Git pushed** to github.com/missecoemma-droid/emma-backup- (all commits: OpenClaw, CH, Supabase, leads API)
- **Supabase tables created** — 5 tables (leads, campaigns, campaign_leads, api_logs, settings) + RLS + grants
- **Logo uploaded** to dashboard at assets/emma-logo.jpg
- **Duplicate createClient bug fixed** in server.js (was crashing on restart after 52 retries)
- **SQLite local cache** added — better-sqlite3 with api_cache, memory_log, cache tables
- **Memory system** — MEMORY.md + memory/2026-07-08.md created in Emma's workspace
- **Telegram webhook fixed** — was pointing to /hooks/wake (OpenClaw), now at /api/telegram-webhook (Express) → bot now responds!
- **Emma status:** Same as Scarlett except Apollo API (missing key)

## What Tony will send
- Emma's personality .md file (SOUL.md replacement)

## 22:05-22:12 UTC — Dashboard bug fix round (Maya reported issues)
- **API rebuilt from scratch** — Server was crashing with duplicate `createClient` + syntax errors from botched PATCH route injection. Full rewrite passed `node -c` syntax check.
- **Missing `node-fetch` and `form-data`** — Earlier API rewrite removed these packages, causing `MODULE_NOT_FOUND`. Re-installed them → API back online.
- **Task column name mismatch** — Supabase tasks table uses `description` not `detail`, and `due_date` not `due`. Fixed POST handler to accept both `description` (frontend) and `detail` (curl), mapping to `description` in DB. GET handler maps `description→detail` and `due_date→due` for frontend compatibility.
- **Maya's console errors:** `loadProjects`, `loadCompliance`, `loadAllocations` — Cannot set innerHTML of null
  - **Root cause:** Git restore (commit `229a502`) erased the HTML page sections for Projects, Compliance, and Work Allocations. Only their JS load functions existed — no `<tbody id="projectsTableBody">` etc.
  - **Fix:** Added full `<section>` blocks with tables for all 3 pages, plus sidebar `<nav-item>` buttons for projects, compliance, work allocation.
- **All fixed and deployed.** Maya taking a break — will test later.

## Pending — Emma (post-fixes, 22:13 UTC)
- Maya needs to hard refresh (Ctrl+F5) and test: login, tasks (save + display), projects, compliance, work allocations, campaigns
- SQL fix file at /root/emma-workspace/fix-table-schema.sql needs Maya to paste in Supabase SQL Editor (adds missing columns to projects and compliance tables)
- Tony's personality file still pending for Emma's SOUL.md
