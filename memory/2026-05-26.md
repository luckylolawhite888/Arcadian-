# 2026-05-26 — Out & About Web App Launch

## Domain Setup
- 07542666646.com pointed to IONOS server 212.227.93.74 via DNS A record
- nginx config written, Let's Encrypt SSL cert obtained (expires 2026-08-24)
- Listify UK React SPA deployed as landing page at https://07542666646.com

## Out & About Web App
- Maya provided her own design file (outandabout_v4.html) — single-file mobile-first app
- She explicitly said: "all you've got to do is link the back end" — no design changes
- Supabase project set up: mdleurcenwmmenvkwjhl.supabase.co
  - Keys: anon (public) + service_role (secret)
  - Tables already existed: stores, deals, favourites, store_memberships, deal_views, deal_redemptions, social_posts, deal_repeats
  - Auth enabled (email signup, auto-confirm was off — need to check if mailer_autoconfirm was true)
- **Fixed:** supabaseClient.createClient → window.supabase.createClient
- **Deployed:** https://07542666646.com/out-and-about/
- **Seed data:** 7 stores and 15 deals populated in Supabase DB

## Issues & Fixes
- First deployment had `supabaseClient.createClient` — UMD export is `window.supabase`, not `supabaseClient`
- Rebuilt from Maya's original file to avoid accidental design changes
- Removed duplicate old doLogin/doRegister/doLogout functions from original

## Email Check (from heartbeat)
- [ ] Need to check inbox
# 2026-05-26

## Morning Briefing Failure (10:00 AM)
- Cron job timed out — WhatsApp delivery chat ID wrong (`17167118545150@lid` instead of `447542666646@c.us`)
- Missing content generation issue: weather, news, numerology, psychology all failed to generate
- Briefing never delivered to Maya on WhatsApp
- **Fix needed:** Update the cron's WhatsApp chat ID

## Heartbeat Checks (Afternoon & Evening)
- 14:27 UTC - Afternoon check: All systems nominal
- 21:37 UTC - Pre-compaction flush: WAHA 401 on direct API calls (API key may differ)
- Website status: 07542666646.com serving both Listify UK and Out & About app

## Maya's Bug Reports & Fixes — Out & About App
- **17:09 UTC** — Maya reported "nothing works, nothing clicks" on Out & About app
  - Root cause: **Duplicate `let timerSecs` declaration** in global scope. Original file had `let timerSecs = DEALS.map(...)` in state section; my Supabase integration added another `let timerSecs = []` before it. Two `let` declarations for same variable in same scope throws SyntaxError — all JS execution halted silently.
  - Fix: Changed second occurrence to `timerSecs = DEALS.map(...)` (assignment only)
  - Verified: `node --check` passes, grep confirms only one `let timerSecs`

- **21:26 UTC** — Maya's detailed bug report:
  1. ❌ "Invalid API key" on signup
     - Root cause: Deployed HTML had `eyJhbGciOiJIUzI1NiIs...` (a placeholder JWT token) instead of the real Supabase publishable key. My `apply_supabase.mjs` script wrote a placeholder and I never replaced it with Maya's actual key.
     - Fix: Replaced with `sb_publishable_mPjat_GLHfK1TwzPxEa2tQ_OvutOgDL`
  2. ❌ No back navigation on drill-down screens
     - Fix: Added `←` back buttons to Login, Saved, Alerts, Profile, and Shop Dashboard screens
     - Also fixed auth code referencing wrong field IDs (`login-email`/`login-pass` → `li-email`/`li-pass`)
  3. ❌ No location access
     - Fix: Added geolocation request when navigating to map view
  4. ❌ "Email rate limit exceeded" on signup
     - Remaining Supabase rate limit issue — added better error messaging
  5. ❌ Deal clicks do nothing
     - Fix: Wired up deal card click → openDeal() detail view
  6. ❌ Need share/redeem/QR code flow
     - Added: 💬 Share via native share sheet (WhatsApp, iMessage), ❤️ Save to favourites, Redeem with QR code, Buy Now flow, Open in Maps, Rate shop

## Other Events
- Maya sent Ruflo (GitHub: ruvnet/ruflo) — agent orchestration platform, MIT licensed, formerly Claude Flow
  - Cool for future multi-agent swarms but not an immediate fit (built for Claude Code, we use DeepSeek)
  - Maya says "keep in mind"
- WhatsApp: WAHA bridge returning 401 on direct API calls — may need key rotation
  - Bridge itself is working (inbound messages still arriving)
- No new The Rundown emails today (last one May 17)
- No calendar events noted
